session: update_brew_mise

root: ~

windows:
  - name: update_brew_mise
    layout: main-vertical
    commands:
      - mise outdated && mise up
    panes:
      - type: vertical
        commands:
          - brewupx
