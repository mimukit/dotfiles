session: system_updates

root: ~

windows:
  - name: system_updates
    layout: main-horizontal
    commands:
      - brewupx
    panes:
      - type: horizontal
        commands:
          - fisher update
          - exit
      - type: horizontal
        commands:
          - mise outdated && mise up
          - exit
