# /Users/mukit/.config/tmuxinator/upx.yml

name: system_updates
root: ~/
windows:
  - system_updates:
      layout: even-vertical
      panes:
        - brew_update:
            - brewupx
        - topgrade:
            - topgrade
        - misc:
            - atuin sync
            - exit
